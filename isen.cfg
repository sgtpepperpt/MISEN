// generic
use_text = 1
use_images = 0

bisen = {
    nr_docs = 10000;
};

visen = {
    train_technique = "client_kmeans";
    train_mode = "train";
    //train_mode = "load";
    search_mode = "inria";

    descriptor_threshold = 500;
    nr_clusters = 5000;
    clusters_file = "centroids5000";
};
