// generic
use_text = 0
use_images = 1
use_multimodal = 0

bisen = {
    nr_docs = 1000000;

    queries = (
        "time", "person", "year", "way", "day", "thing", "man", "world", "life", "hand", "part", "child",
        "history", "country", "born", "lisbon", "york", "paris",

        "time && person", "time && person && year && way && day", "time && person && year && way && day && thing && man && world && life && hand",
        "time || person", "time || person || year || way || day", "time || person || year || way || day || thing || man || world || life || hand",

        "(time && person) || (year && way)", "(time && person) || (year && way) || (day && thing) || (man && world)", "(time && person) || (year && way) || (day && thing) || (man && world) || (life && hand) || (part && child)",
        "(time || person) && (year || way)", "(time || person) && (year || way) && (day || thing) && (man || world)", "(time || person) && (year || way) && (day || thing) && (man || world) && (life || hand) && (part || child)",

        "!time && person && year && way && day && thing && man && world && life && hand",
        "!time && !person && !year && !way && !day && thing && man && world && life && hand",
        "!time && !person && !year && !way && !day && !thing && !man && !world && !life && !hand",
        "!(time && person && year && way && day && thing && man && world && life && hand)"
    );
    doc_type = "wiki";
    dataset_dir = "/home/guilherme/Datasets/wikipedia-parsed/";
};

visen = {
    nr_docs = 1491;
    train_technique = "client_kmeans"; // client_kmeans or iee_kmeans
    train_mode = "load"; // train or load
    add_mode = "normal"; // normal or load
    search_mode = "inria";

    descriptor_threshold = 500;
    nr_clusters = 5000;
    clusters_file = "centroidsbak/centroids5000";
    dataset_dir = "/home/guilherme/Datasets/inria/";
};

misen = {
    text_dir = "/home/guilherme/Datasets/mirflickr/meta/tags/";
    img_dir = "/home/guilherme/Datasets/mirflickr/";
    nr_docs = 10;
    nr_queries = 5;
};

/*
    queries = ("time", "person", "year", "way", "day", "thing", "man", "world", "life", "hand", "part", "child");
    queries = ("history", "country", "born", "lisbon", "york", "paris");

    queries = (
        "time && person",
        "time && person && year && way && day",
        "time && person && year && way && day && thing && man && world && life && hand",
        "time || person",
        "time || person || year || way || day",
        "time || person || year || way || day || thing || man || world || life || hand"
    );

    queries = (
        "!time && person && year && way && day && thing && man && world && life && hand",
        "!time && !person && !year && !way && !day && thing && man && world && life && hand",
        "!time && !person && !year && !way && !day && !thing && !man && !world && !life && !hand",
        "!(time && person && year && way && day && thing && man && world && life && hand)"
    );

    queries = (
        "(time && person) || (year && way)", "(time && person) || (year && way) || (day && thing) || (man && world)", "(time && person) || (year && way) || (day && thing) || (man && world) || (life && hand) || (part && child)",
        "(time || person) && (year || way)", "(time || person) && (year || way) && (day || thing) && (man || world)", "(time || person) && (year || way) && (day || thing) && (man || world) && (life || hand) && (part || child)"
    );
*/
